2018.2.4破解
Windows7成功  Windows10失败
https://blog.csdn.net/Hack_Different/article/details/82852857

打开IDE的新特新提示
Ctrl N  查找打开类
Ctrl 空格 补全输入
Alt F7(在弹出菜单中编辑|查找|查找用法)，您可以快速找到在整个项目中使用特定的类、方法或变量的所有位置。
Ctrl+Q(查看|快速文档)。
Ctrl+B 查看对象的类以及集成实现关系
Ctrl+F12  您可以使用Ctrl+F12(导航|文件结构)快速浏览当前编辑的文件。它显示当前类的成员列表。选择要导航到的元素并按Enter键或F4键。要轻松地在列表中找到某个项目，只需输入其名称即可。
Shift+F6  您可以轻松地重命名类、方法和变量，并自动纠正所有使用它们的地方。要尝试这样做，将插入符号放在要重命名的符号上，并按Shift+F6 (Refactor | rename)。在弹出窗口中键入新名称，或选择建议的名称之一，然后按Enter。
您可以通过按Ctrl+O(代码|重写方法)轻松地重写基类的方法。要实现当前类实现的接口的方法(或抽象基类的方法)，使用Ctrl+I(代码|实现方法)。


当使用代码完成时，您可以使用Tab键接受弹出列表中当前突出显示的选择。与接受Enter键不同，所选名称将覆盖插入符号右侧的名称其余部分。这对于用另一个方法或变量名替换另一个方法或变量名特别有用。

你知道你可以在编辑器和工具窗口中关闭标签，而不使用上下文菜单命令吗?只需用鼠标光标指向要关闭的选项卡，然后单击鼠标中键，或者使用Shift+click组合即可。

Alt+Insert(代码为| Generate)，您可以轻松地为类的任何字段生成getter和setter方法。

要快速在任意视图(项目视图、结构视图或其他视图)中选择当前编辑的元素(类、文件、方法或字段)，请按Alt+F1。

在所有的树视图中都可以找到speed搜索:只需开始输入，就可以快速找到所需的项。

任何工具窗口中的Esc键将焦点移动到编辑器。Shift+Esc将焦点移到编辑器，并隐藏当前(或最后一个活动)工具窗口。F12键将焦点从编辑器移到最后一个焦点工具窗口。

当您想要捕获由代码片段抛出的异常时，在编辑器中选择它，按Ctrl+Alt+T(代码|环绕)并选择try / catch。块中抛出的所有异常的捕获块将自动生成。您可以在文件|设置|文件和代码模板的Code选项卡上定制生成的catch块的主体。使用列表中的其他项来围绕其他结构。

 
实例化代码      StringBuffer buffer = new        and press Ctrl+Shift+空格:   -> StringBuffer buffer = new StringBuffer();

要导航到抽象方法的实现，请将插入符号定位到声明中的用法或名称，并按Ctrl+Alt+B。

编辑器中的Ctrl+W(扩展选择)选择插入符号中的单词，然后选择源代码的扩展区域。例如，它可以选择一个方法名，然后是调用该方法的表达式，然后是整个语句，然后是包含块，等等。您还可以通过双击编辑器中的目标区域来选择插入符号中的单词和扩展的源代码区域。

 
myEditorPane.setBorder(BorderFactory.createEmptyBorder(5,5,5,5)); and press Ctrl+Alt+V (Refactor | Extract | Variable...). This will result in the following:

您可以使用Ctrl+/和Ctrl+Shift+/对代码行和块进行注释或取消注释。使用单行注释(//…)取消当前行或选定块的注释。Ctrl+Shift+/将选中的块括在一个块注释中(/*…*/)。要取消注释，请按Ctrl+Shift+/在其内部的任何地方。

要在编辑器的插入符号处打开带有元素文档的浏览器，请按Shift+F1(查看|外部文档)。要使用此特性，必须在文件|设置| Web浏览器选项和添加到项目(文件|项目结构…)的文档文件路径中设置到浏览器的路径。

当您需要将表达式值转换为所需的类型时，SmartType代码完成将会帮助您。例如,类型字符串s =(<插入符号在这里是>并按Ctrl + Shift +空格看看会发生什么。

编辑器中的Ctrl+D在没有选中任何块时复制所选块或当前行。

String [] array1,array2;
itar

private FileOutputStream and press Ctrl+空格

如果游标位于方法调用的括号之间，按Ctrl+P会弹出一个有效参数列表。

Ctrl+Shift+Backspace(导航|最后的编辑位置)将您带回到您在代码中进行更改的最后一个位置。按下Ctrl+Shift+Backspace几次会让你更深入地了解更改历史。
使用Ctrl+Shift+F7(编辑|查找文件中的|高亮显示用法)快速高亮显示当前文件中某个变量的用法。
使用F3和Shift+F3键来导航突出显示的用法。
按Esc删除高亮显示。

使用代码|重新格式化代码以根据您的代码样式首选项重新格式化代码(文件|设置|代码样式)。您还可以使用代码|优化导入来自动优化导入(删除未使用的导入，等等)。要访问相应的设置，请使用文件|设置|代码样式|导入。



To see your local history of changes in a file, invoke Local History | Show History from the context menu. You can navigate through different file versions, see the differences and roll back to any previous version. Use the same context menu item to see the history of changes on a directory. You will never lose any code with this feature!
要查看文件中更改的本地历史，请从上下文菜单中调用本地历史|显示历史。您可以浏览不同的文件版本，查看差异并回滚到以前的任何版本。
使用相同的上下文菜单项查看目录上的更改历史记录。使用这个特性，您将永远不会丢失任何代码!


按Alt+Q(查看|上下文信息)查看当前方法的声明，而不需要滚动到它。

Ctrl+E(查看|最近的文件)带来最近访问的文件的弹出列表。选择所需的文件并按回车键打开它。


使用F2/Shift+F2键在突出显示的语法错误之间跳转。
使用Ctrl + Alt +向上箭头/ Ctrl + Alt +向下箭头之间的快捷键跳编译错误信息或搜索操作的结果。
若要跳过警告，请右键单击验证侧栏/标记栏，并选择转到高优先级问题。


使用Ctrl+J完成任何有效的活动模板缩写，如果你不记得它。例如，键入它并按Ctrl+J看看会发生什么。

要在编辑器中显示方法之间的分隔线，打开编辑器设置并在外观页面中选择show method separators复选框。

当使用基本代码完成(Ctrl +空格),输入任何字符存在一个标识符。

使用Alt +向上箭头和Alt +向下箭头键之间快速移动方法在编辑器中。

Ctrl+Shift+J快捷键将两行合并为一行，并删除不必要的空间以匹配您的代码风格。

使用Refactor | Copy创建一个类，该类是所选类的副本。例如，当您需要创建一个与某些现有类有许多共同之处的类，并且无法将共享功能放到公共超类中时，这可能非常有用。

使用Ctrl+Shift+V快捷键选择并插入最近的剪贴板内容到文本。

要查看所选类的继承层次结构，请按Ctrl+H(导航|类型层次结构)。您还可以从编辑器中调用hierarchy视图来查看当前编辑的类的层次结构。


Debug
右键单击断点标记(在文本左侧的栏上)将调用speedmenu，在这里您可以快速启用/禁用断点或调整其属性。

要在调试程序时轻松地评估任何表达式的值，请在编辑器中选择其文本(可以多次按Ctrl+W来有效地执行此操作)，并按Alt+F8。

要在调试程序时快速评估任何表达式的值，按住Alt键并单击该表达式查看其值并计算它，调用方法等。

使用基本完成(Ctrl +空格)完成单词在评论文本和许多不同类型的文件。当前文件中以类型化前缀开头的所有单词都将出现在查找列表中。

快捷方式如Ctrl+Q(查看|快速文档)、Ctrl+P(查看|参数信息)、Ctrl+B(导航|声明)等不仅可以在编辑器中使用，还可以在代码完成弹出列表中使用。

在使用代码完成时，您可以使用句点字符(.)、逗号(、)、分号(;)、空格和其他字符接受弹出列表中当前突出显示的选择。选择的名称在编辑器中自动输入，后面跟着输入的字符。

为了帮助您了解主菜单中每个项目的用途，当您将鼠标指针放在该项目上时，它的简短描述显示在应用程序框架底部的状态栏中。

对于在Go to类中的模式搜索，转到Symbol和文件弹出框，使用*和空格符号。*代表任何符号。末尾的空格表示模式的结束，前面的字符串将不只是前缀，而是整个模式。建议名单将相应减少。

当您在声明为静态的嵌套类上调用Move重构(F6)时，您可以选择将其设置为顶级类，或者将其移到其他类。

您甚至可以在不完整的语句上使用Extract变量重构。只需按Ctrl+Alt+V，选择所需的表达式:

要在编辑器中快速打开任何特定的方法或字段，请按Ctrl+Alt+Shift+N(导航|符号)并开始键入其名称。从出现的下拉列表中选择symbol。

使用Alt+Shift+C快速检查您最近对项目的更改。

使用基本完成(Ctrl +空格)的HTML,CSS和其他文件,完成图像文件的名字。

您可以开始引用Ant属性或目标，即使它还没有定义。一个意图动作特性将建议您自动创建必要的标记，而无需离开当前编辑位置。

只需一个按键，您就可以从编辑器中应用另一个代码样式/着色方案或密钥映射。只需按Ctrl+ '(查看|快速切换方案)，指定要更改的方案。









